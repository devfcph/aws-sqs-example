cloud:
  aws:
    region:
      static: us-east-1
      auto: false
    credentials:
      access-key: access-key
      secret-key: secret-key
    end-point:
      uri: my-endpoint
server:
  port: 8081